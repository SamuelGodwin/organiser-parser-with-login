package model;

/**
 * enum 'TokenType' for set of fixed constants 'DATE', 'TIME', 'LOCATION' & 'EVENT'.
 * 
 * @author Patrick & Samuel
 *
 */
public enum TokenType {
		DATE, 
		TIME, 
		LOCATION,
		EVENT;
}